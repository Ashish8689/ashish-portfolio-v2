export interface PortfolioProps {
    data: {
        name: string
        img: string
        url: string
        githubUrl: string
        type: string
        stack: string[]
        latest: boolean
        description: string
    }
}
